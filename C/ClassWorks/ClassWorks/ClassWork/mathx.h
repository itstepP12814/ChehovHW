#pragma once //Фишка из вижуал, работает не всегда, т.к нет в стандарте, вставляет заголовочник один раз.
//#ifndef MATHX_H// все что начинается с диеза это дерективы препроцессора
//#define MATHX_H
//#define float int - указание названия макроса и что он делает. А делает он следующее: меняет все флоты на инты.
int gcd(int a, int b);//прототипы функции
int lcm(int a, int b);

//#endif // MATHX_H
